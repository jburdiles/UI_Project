{
    "name": "Temp Cleaner",
    "description": "Finds and removes temporary files, caches and unnecessary files to free up disk space.",
    "inputs": [
        {
            "id": "target_folder",
            "label": "Folder to clean",
            "type": "folder",
            "required": true
        }
    ]
}