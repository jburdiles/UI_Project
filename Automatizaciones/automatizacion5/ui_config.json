{
    "name": "Image Converter",
    "description": "Converts images between different formats (PNG, JPG, GIF, etc.) with resizing and optimization options.",
    "inputs": [
        {
            "id": "input_folder",
            "label": "Folder with images",
            "type": "folder",
            "required": true
        },
        {
            "id": "output_folder",
            "label": "Output folder",
            "type": "folder",
            "required": true
        }
    ]
}